apply plugin: 'com.android.application'

android {
    signingConfigs {
        SignV1 {
            storeFile file('./cert_crPW9s6LcNkn_vptEKH6oEt9K.jks')
            storePassword 'crPW9s6LcNkn'
            keyPassword 'vptEKH6oEt9K'
            keyAlias 'key0'
            v2SigningEnabled false
        }
    }
    compileSdkVersion 27
    buildToolsVersion '28.0.3'
    defaultConfig {
        applicationId "com.fatangare.logcatlogger.example"
        minSdkVersion 14
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.SignV1
    }
    buildTypes {
        debug {
            signingConfig signingConfigs.SignV1
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    testImplementation 'junit:junit:4.12'
    implementation 'com.android.support:appcompat-v7:27.1.1'
    implementation project(':logcatviewer')
}
